version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: schoolnews-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: SchoolNewsDB
    volumes:
      - mongodb_data:/data/db
      - ./init-database.js:/docker-entrypoint-initdb.d/init-database.js:ro
    networks:
      - sn-network

volumes:
  mongodb_data:

networks:
  sn-network:
    driver: bridge